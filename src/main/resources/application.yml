spring:
  application:
    name: artemis-integration
  jms:
    pub-sub-domain: true # true=topic, false=queue
    cache:
      enabled: false # disable cache => connectionFactory won't be wrapped in CachingConnectionFactory (spring class)
  #      session-cache-size: 10

  artemis:
    host: localhost
    port: 61616
    user: admin
    password: admin

app:
  topicName: topic-test-name
  subscription: sub-1
  messageSelector:
