spring:
  application:
    name: artemis-integration
  jms:
    pub-sub-domain: true
    cache:
      enabled: false
  #      session-cache-size: 10
#      enabled: true
  artemis:
    host: localhost
    port: 61616
    user: admin
    password: admin

app:
  topicName: topic-test-name
  subscription: sub-1
  messageSelector:
