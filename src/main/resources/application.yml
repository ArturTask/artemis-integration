spring:
  application:
    name: artemis-integration
  jms:
    cache:
      session-cache-size: 10
      enabled: true
  artemis:
    host: localhost
    port: 61616
    user: admin
    password: admin
